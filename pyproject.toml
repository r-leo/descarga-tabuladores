[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "descarga_tabuladores"
authors = [
    {name = "Rodrigo Leo", email = "rodrigo.leo@bbva.com"},
]
dependencies = [
    "requests",
    "openpyxl"
]
